import { DisabledRightClickDirective } from './disabled-right-click.directive';

describe('DisabledRightClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledRightClickDirective();
    expect(directive).toBeTruthy();
  });
});
