import { IpoMyApplicationPipe } from './ipo-my-application.pipe';

describe('IpoMyApplicationPipe', () => {
  it('create an instance', () => {
    const pipe = new IpoMyApplicationPipe();
    expect(pipe).toBeTruthy();
  });
});
